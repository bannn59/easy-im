# 附录：非产品表面（勿计入 IM 功能地图）

以下存在于仓库，但是 **开发者/Agent 工作流**，不是终端用户 IM 功能。

| 区域 | 内容 |
|------|------|
| `.trellis/` | workflow、scripts、spec、tasks、workspace |
| `.claude/` / `.codex/` / `.cursor/` / `.agents/` | 平台 agents、hooks、skills |
| `AGENTS.md` | Trellis 说明 |

**统计时已排除**；避免把「能跑 trellis-implement」误报成产品能力。
